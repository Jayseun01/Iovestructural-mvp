"""
iLoveStructural - Unified Engineering Tools Platform
Professional design with background images and slideshow
"""

import streamlit as st
from datetime import datetime
import time

# ============================================================================
# PAGE CONFIG
# ============================================================================

st.set_page_config(
    page_title="iLoveStructural - Engineering Tools",
    page_icon="🏗️",
    layout="wide",
    initial_sidebar_state="collapsed",
)

# ============================================================================
# CUSTOM CSS WITH BACKGROUND IMAGES
# ============================================================================

st.markdown("""
<style>
    /* Main container */
    .main {
        padding: 0;
    }
    
    /* Header with background image */
    .header {
        background: linear-gradient(rgba(102, 126, 234, 0.85), rgba(118, 75, 162, 0.85)), 
                    url('https://images.unsplash.com/photo-1552664730-d307ca884978?w=1600&q=80');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        color: white;
        padding: 6rem 2rem;
        text-align: center;
        border-bottom: 4px solid #667eea;
        position: relative;
        min-height: 500px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    
    .header h1 {
        font-size: 4rem;
        margin: 0;
        font-weight: 900;
        letter-spacing: -2px;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    
    .header p {
        font-size: 1.4rem;
        margin: 1rem 0 0 0;
        opacity: 0.98;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
    }
    
    /* Slideshow container */
    .slideshow-container {
        max-width: 100%;
        position: relative;
        background: #000;
        height: 400px;
        overflow: hidden;
        margin-bottom: 3rem;
    }
    
    .slide {
        display: none;
        width: 100%;
        height: 100%;
        position: relative;
    }
    
    .slide.active {
        display: block;
        animation: fade 0.5s;
    }
    
    @keyframes fade {
        from {opacity: 0.4}
        to {opacity: 1}
    }
    
    .slide img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
    
    .slide-caption {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(transparent, rgba(0,0,0,0.7));
        color: white;
        padding: 2rem;
        font-size: 1.1rem;
        font-weight: 600;
    }
    
    .slide-dots {
        text-align: center;
        padding: 1rem 0;
    }
    
    .dot {
        height: 12px;
        width: 12px;
        margin: 0 5px;
        background-color: #bbb;
        border-radius: 50%;
        display: inline-block;
        cursor: pointer;
        transition: background-color 0.3s;
    }
    
    .dot.active {
        background-color: #667eea;
    }
    
    /* Tools container */
    .tools-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        padding: 3rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
        background: linear-gradient(to bottom, #f8f9fa, #ffffff);
    }
    
    /* Tool card with hover effects */
    .tool-card {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        cursor: pointer;
        border: 2px solid transparent;
        height: 100%;
        display: flex;
        flex-direction: column;
        position: relative;
        overflow: hidden;
    }
    
    .tool-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
        transition: left 0.5s;
    }
    
    .tool-card:hover::before {
        left: 100%;
    }
    
    .tool-card:hover {
        transform: translateY(-12px);
        box-shadow: 0 20px 40px rgba(102, 126, 234, 0.3);
        border-color: #667eea;
    }
    
    .tool-icon {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        text-align: center;
    }
    
    .tool-name {
        font-size: 1.6rem;
        font-weight: 700;
        color: #333;
        margin: 1rem 0 0.5rem 0;
    }
    
    .tool-desc {
        color: #666;
        font-size: 0.95rem;
        line-height: 1.6;
        margin-bottom: 1.5rem;
        flex-grow: 1;
    }
    
    .tool-status {
        display: inline-block;
        padding: 0.6rem 1.2rem;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
        margin-top: auto;
    }
    
    .status-active {
        background: #e8f5e9;
        color: #2e7d32;
    }
    
    .status-new {
        background: #fff3e0;
        color: #e65100;
    }
    
    /* Tool page header */
    .tool-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%), 
                    url('https://images.unsplash.com/photo-1552664730-d307ca884978?w=800&q=80');
        background-size: cover;
        background-position: center;
        background-blend-mode: overlay;
        color: white;
        padding: 3rem 2rem;
        border-radius: 12px;
        margin-bottom: 2rem;
        display: flex;
        align-items: center;
        gap: 2rem;
    }
    
    .tool-header h1 {
        margin: 0;
        font-size: 2.5rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    
    .tool-header-icon {
        font-size: 3.5rem;
    }
    
    /* Footer */
    .footer {
        background: linear-gradient(135deg, #333 0%, #555 100%);
        color: #fff;
        padding: 3rem 2rem;
        text-align: center;
        margin-top: 3rem;
    }
    
    .footer p {
        margin: 0.5rem 0;
    }
    
    .footer-divider {
        height: 2px;
        background: #667eea;
        margin: 1rem auto;
        width: 100px;
    }
    
    /* Stats section */
    .stats-section {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 2rem;
        border-radius: 12px;
        margin: 2rem 0;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 2rem;
        text-align: center;
    }
    
    .stat-item h3 {
        font-size: 2rem;
        margin: 0;
    }
    
    .stat-item p {
        margin: 0.5rem 0 0 0;
        opacity: 0.9;
    }
    
</style>
""", unsafe_allow_html=True)

# ============================================================================
# SESSION STATE FOR NAVIGATION
# ============================================================================

if "current_page" not in st.session_state:
    st.session_state.current_page = "home"

if "current_slide" not in st.session_state:
    st.session_state.current_slide = 0

# ============================================================================
# ENGINEERING IMAGES FOR SLIDESHOW
# ============================================================================

SLIDES = [
    {
        "image": "https://images.unsplash.com/photo-1552664730-d307ca884978?w=1600&q=80",
        "caption": "Precision Engineering Design - Creating structural excellence"
    },
    {
        "image": "https://images.unsplash.com/photo-1453614512568-c4024d13c247?w=1600&q=80",
        "caption": "Professional CAD Drafting - Every detail matters"
    },
    {
        "image": "https://images.unsplash.com/photo-1552664730-d307ca884978?w=1600&q=80",
        "caption": "Structural Analysis & Design - Safety first"
    },
    {
        "image": "https://images.unsplash.com/photo-1517694712202-14dd9538aa97?w=1600&q=80",
        "caption": "Engineering Solutions - Building the future"
    },
    {
        "image": "https://images.unsplash.com/photo-1552664730-d307ca884978?w=1600&q=80",
        "caption": "Quality Assurance - Standards compliance guaranteed"
    },
]

# ============================================================================
# TOOL DEFINITIONS
# ============================================================================

TOOLS = {
    "dxf_purge": {
        "name": "DXF Purge Tool",
        "icon": "🧹",
        "description": "Clean and optimize DXF files by removing unused layers, blocks, text styles, and other redundant elements. Reduce file size and improve performance.",
        "status": "active",
        "module": "tools.dxf_purge_tool",
    },
    "smart_grid": {
        "name": "Smart Grid Label Sync",
        "icon": "📊",
        "description": "Automatically synchronize grid labels across your drawing. Ensure consistency and avoid manual label updates with intelligent labeling system.",
        "status": "active",
        "module": "tools.smart_grid_label_sync",
    },
    "smart_beam": {
        "name": "Smart Beam Label Sync Tool",
        "icon": "🌉",
        "description": "Synchronized beam labeling tool for structural drawings. Auto-detect beams and maintain consistent naming conventions across your project.",
        "status": "active",
        "module": "tools.smart_beam_label_sync",
    },
    "plan_model": {
        "name": "Plan Model Suggestion Tool",
        "icon": "🎯",
        "description": "Intelligent tool that suggests optimal structural configurations based on your floor plans. Get design recommendations with a single click.",
        "status": "active",
        "module": "tools.plan_model_suggestion",
    },
    "staircase": {
        "name": "Staircase Designer",
        "icon": "🏗️",
        "description": "Design custom staircases with parametric reinforcement scheduling. Automatically calculates riser heights, rebar patterns, and material quantities.",
        "status": "new",
        "module": "tools.staircase_designer",
    },
}

# ============================================================================
# HOME PAGE
# ============================================================================

if st.session_state.current_page == "home":
    
    # Header with background
    st.markdown("""
    <div class="header">
        <h1>🏗️ iLoveStructural</h1>
        <p>Professional Engineering Design Tools Platform</p>
    </div>
    """, unsafe_allow_html=True)
    
    # Slideshow of engineering images
    st.markdown("<div class='slideshow-container'>", unsafe_allow_html=True)
    
    col1, col2, col3 = st.columns([1, 0.1, 1])
    
    with col2:
        if st.button("⏮", help="Previous"):
            st.session_state.current_slide = (st.session_state.current_slide - 1) % len(SLIDES)
            st.rerun()
    
    slide = SLIDES[st.session_state.current_slide]
    st.markdown(f"""
    <div class='slide active'>
        <img src='{slide['image']}' style='width:100%; height:400px; object-fit:cover;'>
        <div class='slide-caption'>{slide['caption']}</div>
    </div>
    """, unsafe_allow_html=True)
    
    with col2:
        if st.button("⏭", help="Next"):
            st.session_state.current_slide = (st.session_state.current_slide + 1) % len(SLIDES)
            st.rerun()
    
    # Slide indicators
    st.markdown("<div class='slide-dots'>", unsafe_allow_html=True)
    for i in range(len(SLIDES)):
        active_class = "active" if i == st.session_state.current_slide else ""
        st.markdown(f"<span class='dot {active_class}' style='cursor:pointer;'></span>", unsafe_allow_html=True)
    st.markdown("</div>", unsafe_allow_html=True)
    
    st.markdown("</div>", unsafe_allow_html=True)
    
    # Stats section
    st.markdown("""
    <div class='stats-section'>
        <div class='stat-item'>
            <h3>5</h3>
            <p>Powerful Tools</p>
        </div>
        <div class='stat-item'>
            <h3>∞</h3>
            <p>Possibilities</p>
        </div>
        <div class='stat-item'>
            <h3>100%</h3>
            <p>Professional</p>
        </div>
        <div class='stat-item'>
            <h3>24/7</h3>
            <p>Availability</p>
        </div>
    </div>
    """, unsafe_allow_html=True)
    
    st.markdown("")  # Spacer
    
    # Tools grid
    st.markdown("## 🛠️ Our Engineering Tools")
    st.markdown('<div class="tools-grid">', unsafe_allow_html=True)
    
    cols = st.columns(3)
    
    for idx, (tool_id, tool_info) in enumerate(TOOLS.items()):
        col = cols[idx % 3]
        
        with col:
            # Create clickable card
            if st.button(
                f"""
{tool_info['icon']}

**{tool_info['name']}**

{tool_info['description']}
                """,
                key=f"tool_btn_{tool_id}",
                use_container_width=True,
                help=f"Click to open {tool_info['name']}",
            ):
                st.session_state.current_page = tool_id
                st.rerun()
            
            # Status badge
            status_class = "status-new" if tool_info["status"] == "new" else "status-active"
            status_text = "🆕 NEW" if tool_info["status"] == "new" else "✅ ACTIVE"
            
            st.markdown(
                f'<div class="tool-status {status_class}">{status_text}</div>',
                unsafe_allow_html=True
            )
    
    st.markdown("</div>", unsafe_allow_html=True)
    
    # Features section
    st.markdown("---")
    st.markdown("## ✨ Why Choose iLoveStructural?")
    
    col1, col2, col3 = st.columns(3)
    
    with col1:
        st.markdown("""
        ### 🎯 Precision
        Every calculation is based on international standards (IS456, EC2, ACI).
        """)
    
    with col2:
        st.markdown("""
        ### ⚡ Speed
        Generate designs and schedules in seconds, not hours.
        """)
    
    with col3:
        st.markdown("""
        ### 📊 Professional
        Production-ready outputs ready for submission.
        """)
    
    # Footer
    st.markdown(f"""
    <div class="footer">
        <h3>🏗️ iLoveStructural</h3>
        <div class="footer-divider"></div>
        <p><strong>Professional Engineering Design Tools</strong></p>
        <p>© {datetime.now().year} | All Rights Reserved</p>
        <p style="font-size: 0.9rem; color: #999;">Built with ❤️ for engineers, by engineers</p>
    </div>
    """, unsafe_allow_html=True)

# ============================================================================
# TOOL PAGES
# ============================================================================

else:
    tool_id = st.session_state.current_page
    tool_info = TOOLS.get(tool_id, {})
    
    # Back button in sidebar
    with st.sidebar:
        if st.button("← Back to Home", use_container_width=True, key="back_btn"):
            st.session_state.current_page = "home"
            st.rerun()
    
    # Tool header
    st.markdown(f"""
    <div class="tool-header">
        <div class="tool-header-icon">{tool_info.get('icon', '🔧')}</div>
        <div>
            <h1>{tool_info.get('name', 'Tool')}</h1>
            <p>{tool_info.get('description', '')}</p>
        </div>
    </div>
    """, unsafe_allow_html=True)
    
    st.markdown("---")
    
    # Load and run the tool
    try:
        module_name = tool_info.get("module")
        
        if module_name == "tools.dxf_purge_tool":
            from tools.dxf_purge_tool import run
            run()
        
        elif module_name == "tools.smart_grid_label_sync":
            from tools.smart_grid_label_sync import run
            run()
        
        elif module_name == "tools.smart_beam_label_sync":
            from tools.smart_beam_label_sync import run
            run()
        
        elif module_name == "tools.plan_model_suggestion":
            from tools.plan_model_suggestion import run
            run()
        
        elif module_name == "tools.staircase_designer":
            from tools.staircase_designer import run
            run()
        
        else:
            st.error(f"Tool module {module_name} not found")
    
    except ImportError as e:
        st.error(f"Could not load tool: {str(e)}")
        st.info(f"Make sure the tool module exists in the tools/ folder")
    
    except Exception as e:
        st.error(f"Error running tool: {str(e)}")
        st.info("Check the tool's run() function for errors")
